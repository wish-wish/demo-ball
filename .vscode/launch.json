{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "CocosCreator Launch Chrome against localhost",
            "url": "localhost:7457",
            "webRoot": "${workspaceFolder}"
        }
    ]
}